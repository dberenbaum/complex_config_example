countries:
  uk:
    model_params:
      batch_size: 128
      epochs: 1
      latent_dim: 512
      num_samples: 100000
      max_length: 100
    name: uk
  fr:
    model_params:
      batch_size: 32
      epochs: 1
      latent_dim: 128
      num_samples: 1000
      max_length: 100
    name: fr
  es:
    model_params:
      batch_size: 32
      epochs: 1
      latent_dim: 128
      num_samples: 1000
      max_length: 80
    name: es
  us:
    name: us
    models@model_params: large
split: 0.8
